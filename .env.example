# Laravel API (required for blog, auth, tools, chat). Restart dev server after changes.
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# Production example:
# NEXT_PUBLIC_API_URL=https://admin.aashishtimalsina.com.np/api/v1

# Google Analytics 4 → Admin → Data streams → Measurement ID (G-...)
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Optional — only if you verify via HTML meta tag (DNS verification does not need this)
# NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=
